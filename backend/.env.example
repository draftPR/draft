# Backend environment variables
APP_ENV=development
DATABASE_URL=sqlite:///./kanban.db
FRONTEND_URL=http://localhost:5173

# Task backend: "sqlite" (default, no Redis needed) or "redis" (requires Redis)
TASK_BACKEND=sqlite
